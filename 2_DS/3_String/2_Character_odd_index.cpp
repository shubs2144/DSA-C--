/* Find Characters of a string at odd index
 * Arijit = rjt
 * priyanka = ryna
 */

#include <bits/stdc++.h>
using namespace std;

int main()
{
    string name;
    cout << "Enter the name : ";
    cin >> name;

    for (int i = 0; i < name.length(); i++);
    if (i % 2 != 0)
    {
        cout << s[i];
    }
}